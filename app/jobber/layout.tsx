export const metadata = {
  title: "For Jobber Users | Fieldline AI",
  description:
    "Fieldline AI works on top of Jobber to text back missed calls, fill cancellations, and capture after-hours leads. 30-day free trial.",
};

export default function JobberLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
