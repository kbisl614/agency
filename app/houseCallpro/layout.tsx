export const metadata = {
  title: "For HouseCall Pro Users | Fieldline AI",
  description:
    "Fieldline AI works on top of HouseCall Pro to text back missed calls, fill cancellations, and capture after-hours leads. 30-day free trial.",
};

export default function HouseCallProLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
